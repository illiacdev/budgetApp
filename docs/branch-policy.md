# 브랜치 정책

## 기본 규칙
- `main`: 배포 가능 상태만 유지한다.
- `develop`: 통합 브랜치로 사용한다.
- `feature/*`: 기능 개발 브랜치.
- `fix/*`: 버그 수정 브랜치.

## 운영 규칙
- 모든 변경은 `feature/*`에서 시작한다.
- PR은 `develop`으로 먼저 병합한다.
- `main`은 릴리스 또는 안정화 시점에만 갱신한다.
- CI 통과 전 병합 금지.

